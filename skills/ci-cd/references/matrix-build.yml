name: Matrix Build

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest

# ADAPT: TypeScript/Bun
# Replace actions/setup-python@v6 with:
#   - name: Set up Bun ${{ matrix.bun-version }}
#     uses: oven-sh/setup-bun@v2
#     with:
#       bun-version: ${{ matrix.bun-version }}
# Replace matrix python-version with:
#   bun-version: ["1.1.x", "latest"]
# Replace pip install with: bun install
# Replace pytest with: bun test

# ADAPT: Go
# Replace actions/setup-python@v6 with:
#   - name: Set up Go ${{ matrix.go-version }}
#     uses: actions/setup-go@v5
#     with:
#       go-version: ${{ matrix.go-version }}
# Replace matrix python-version with:
#   go-version: ["1.22.x", "1.23.x"]
# Remove pip install step
# Replace pytest with: go test ./...

# ADAPT: Rust
# Replace actions/setup-python@v6 with:
#   - name: Set up Rust
#     uses: dtolnay/rust-toolchain@stable
# Remove matrix python-version (or keep for Rust version matrix: ["stable", "beta"])
# Remove pip install step
# Replace pytest with: cargo test

# ADAPT: Node.js
# Replace actions/setup-python@v6 with:
#   - name: Set up Node.js ${{ matrix.node-version }}
#     uses: actions/setup-node@v6
#     with:
#       node-version: ${{ matrix.node-version }}
# Replace matrix python-version with:
#   node-version: ["20.x", "22.x"]
# Replace pip install with: npm ci
# Replace pytest with: npm test
